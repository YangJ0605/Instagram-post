import styled from 'styled-components'

const Layout = styled.div`
  max-width: 1200px;
  margin: 0 auto;
  padding: 50px 0;
  position: relative;
  .add-btn {
    position: absolute;
    top: 15px;
    right: 15px;
  }
`

export default Layout